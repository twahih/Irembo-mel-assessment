# TeleClinic Clinical Governance Dashboard

Part 4 deliverable — React dashboard for the Clinical Governance Committee.

```bash
npm install
npm run dev
```

Open http://localhost:5173. Data is in `public/data/` (exported from the Excel workbook).
